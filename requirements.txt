# Core analysis dependencies
pandas>=1.3
numpy>=1.21
matplotlib>=3.4
seaborn>=0.11

# To open and run the notebook in VS Code / Jupyter
jupyter>=1.0

# Only needed if you want to rebuild the PDF report (report_gen.py)
reportlab>=3.6
Pillow>=9.0
